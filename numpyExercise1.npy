import numpy as sash

array1 = [1,2,3]
array2 = [4,5,6]
#printing bitwisie or and --1
res = sash.bitwise_or(array1, array2)
res2=sash.bitwise_and(array1,array2)

print(res)
print(res2)

#dimension of array --2
print(np.ndim(array1))

#creating array with 2 dimension --3
arr = np.array([1, 2, 3, 4], ndmin=2)
print(arr)

#print 50 --4
arr = np.array([[10, 20, 30, 40], [50, 60, 70, 80]])
print(arr[1,0])

res3=sash.invert(num)
print(res3)
res = sash.invert(array1)
res2=sash.invert(array2)
print(res)
print(res2)
